// No longer used by the app itself (ratings are now tied to real User
// accounts, see routes/ratings.js). Kept as a reference for which
// displayName each family member's account should get when created with
// createUser.js, and as the matching list for
// scripts/migrateRatingsToUsers.js.
export const ratingUsers = [
  "Imants (35+ gadi)",
  "Santa (35+ gadi)",
  "Toms (11+ gadi)",
  "Paula (8+ gadi)",
];
